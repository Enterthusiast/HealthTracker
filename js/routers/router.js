"use strict";

var app = app || {};

// TODO: We could create a 404 error page

// Backbone app router
var Workspace = Backbone.Router.extend({

    routes:{

        // 'article/:id': 'getContentsList',

    },

    // // Function called to trigger an event
    // // asking to display the contents of an article
    // getContentsList: function (id) {

    //     console.log('get contents from article:', id);
    //     this.trigger('loadcontents', id);

    // }

});